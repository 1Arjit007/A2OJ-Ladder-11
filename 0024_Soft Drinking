#include <bits/stdc++.h>
using namespace std;

int main() 
{
    int n, k, l, c, d, p, nl, np;
    cin>>n>>k>>l>>c>>d>>p>>nl>>np;
    int r1=(k*l)/nl;
    int r2=(c*d);
    int r3=(p/np);
    cout<<(min(r1,min(r2,r3))/n);
	return 0;
}
